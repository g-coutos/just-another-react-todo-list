import { HeaderWrapper, HeaderTitle } from "./styles";

export const Header = () => {
  return (
    <HeaderWrapper>
      <HeaderTitle>just-another-react-todo-list</HeaderTitle>
    </HeaderWrapper>
  );
};
